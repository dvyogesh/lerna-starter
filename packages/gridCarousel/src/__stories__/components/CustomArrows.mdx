
import { Meta, Preview } from '@storybook/addon-docs/blocks';
import GridCarousel from '../../';
import gridCarouselMockData from '../../../../../public/__mock__/gridCarouselMockData';
import { StyledSubPara, MessageWrappeStyle } from '../../../../../public/styles/globalStyle';
import {CodeWrapper} from '../../../../../public/jsUtil/GlobalWrappers';
import CarouselItem from '../../components/CarouselItem';
import Arrows from '../../components/Arrows';
const customOnclick = () => {
    console.log('i am customOnclick')
}
<Meta title="Custom Arrows  GridCarousel" component={GridCarousel} />

## Custom Arrows

<StyledSubPara>
  To Customize Arrows or to add own arrows, need to pass <code>customArrows</code> prop.
</StyledSubPara>

<MessageWrappeStyle>
  <code>customArrows</code> prop should be Object
</MessageWrappeStyle>


<CodeWrapper>
  <div className="codeIn">
  {`
   import GridCarousel from '@yoge-shource/grid-carousel-react
   
   import Arrow from '---your arrow---'
   
    First Way
    
    <GridCarousel
      itemComponent={CarouselItem} // your component
      carouselData={gridCarouselMockData}
      // --- here ---
      customArrows={{
          leftArrow: Arrows, // your Arrow component and do not <Arrow />
          leftArrowProps: {left:'right', square:'square'},  // your Arrow component props
          rightArrow: Arrows, // your Arrow component props
          rightArrowProps:{right:'right', square:'square'} // your Arrow component props
        }
      }
    />
    
    ---- OR ---- 
    Second Way
    
    <GridCarousel
      carouselData={gridCarouselMockData} 
      // --- here ---
      customArrows={{ // double braces important
          leftArrow: Arrows, // your Arrow component and do not <Arrow />
          leftArrowProps: {left:'right', square:'square', onClick:onClickHandler}, 
           // onClick: () => onClickHandler() ---- wrong---- 
            // onClick: onClickHandler---- right----
          rightArrow: Arrows,
          rightArrowProps:{right:'right', square:'square', onClick:onClickHandler}
        }} >
       {
        gridCarouselMockData.length > 0 && 
          gridCarouselMockData.map((item, itemKey) => (
            <div className="carousel-item">
                <CarouselItem item={item}/> // your component
            </div>
        ))
       }
    </GridCarousel>
  `} 
  <MessageWrappeStyle>
    In side GridCarousel  Above Arrow will render like this 
  </MessageWrappeStyle>
  {
      `
      <Arrows right='right', square='square' onClick={()=>onClick()} />
      `
  }
  </div>
  
 
</CodeWrapper>



<Preview>
  <GridCarousel
    itemComponent={CarouselItem}
    carouselData={gridCarouselMockData}
    customArrows={{
      leftArrow: Arrows,
      leftArrowProps: {left:'right', square:'square',onClick:customOnclick},  
      rightArrow: Arrows,
      rightArrowProps:{right:'right', square:'square', onClick:customOnclick}
    }
    }
  />
</Preview>

